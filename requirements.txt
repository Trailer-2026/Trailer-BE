fastapi==0.110.1
SQLAlchemy==2.0.36
uvicorn==0.29.0
python-multipart==0.0.9
python-dotenv==1.0.1
requests==2.31.0
psycopg2-binary          # 추가됨 (PostgreSQL 필수)
google-cloud-storage     # 추가됨 (GCP 스토리지 필수)
aiofiles                 # 추가됨 (정적 파일/ReDoc용)
pydantic-settings        # 추가됨 (설정 관리용)
httpx==0.27.0
pydantic-settings==2.2.1
PyJWT==2.9.0            # 추가됨 (자체 JWT 발급/검증용)
firebase-admin          # 추가됨 (FCM 푸시 알림)

# --- services/videoMaker 렌더 엔진 (POST /api/videos/render) ---
# [videomaker] python 으로 별도 환경(trailer3d)을 지정하면 아래는 그 환경에만 있으면 된다.
playwright>=1.50,<2     # local 엔진 (설치 후 `playwright install chromium` 필요)
Pillow>=10,<13          # 사진 합성·인트로/아웃트로 클립
modal>=1.5,<2           # Modal T4 클라우드 렌더 엔진 (modal 로그인 필요)
protobuf<7              # modal(<7 요구)과 firebase-admin 계열 공존을 위한 상한 핀